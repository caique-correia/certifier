import { Header, Form, Footer } from "@/components/login"

export default function Login(){
    return (
        <main>
            <Header/>
            <Form/>
            <Footer/>
        </main>
    )
}